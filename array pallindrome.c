// write a c program to take an array from user and traverse that array if element is prime then convert it in to next pallindrome number and if element is composite convert it into next armstrong number .
#include<stdio.h>

